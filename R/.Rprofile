# ref Jenny Bryan: https://usethis.r-lib.org/articles/usethis-setup.html
#   personalize accordingly
options(
  usethis.full_name = "Jane Doe",
  usethis.description = list(
    "Authors@R" = utils::person(
        "Jane", "Doe",
        email = "jane@example.com",
        role = c("aut", "cre"),
        comment = c(ORCID = "JANE'S-ORCID-ID")
    ),
    Version = "0.0.0.9000"
  ),
  usethis.destdir = "~/the/place/where/I/keep/my/R/projects",
  usethis.overwrite = TRUE
)

if (interactive()) {
  # uncomment if you want to load {devtools} and {usethis}
  # automatically in your interactive sessions
  
  # suppressMessages(require("devtools"))
}
